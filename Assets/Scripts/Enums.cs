using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum GameplayState { StartUp, InGame, Shop, Paused, GameOver }

public enum PlayerMagicState { Ice, Fire, Lightning, Earth }